import V1_0_0 from "./V1_0_0";

/**
 * JsonResume Schema v1.0.0 (https://jsonresume.org) converted into TypeScript type-interface.
 * The intent is to be backward compatible to this schema but will be extended with additional features since the
 * current implementation is rather shallow for data systems that go beyond visual representations.
 */
export type JsonResume = V1_0_0;
