import Component from "./index";

import { Meta, Story } from "@storybook/addon-docs";

import PaperPreview from "@cv/components/utils/PaperPreview.tsx";

<Meta title="@cv/Containers/Record" component={Component} />

<!--👇 We create a “template” of how args map to rendering -->

export const Template = ({ wide, ...args }) => (
  <PaperPreview wide={wide}>
    <Component {...args} />
  </PaperPreview>
);

<!-- 👇 The args you need here will depend on your component -->

<!-- TODO: Turn into Compositions https://storybook.js.org/docs/react/writing-stories/build-pages-with-storybook -->

<Story
  name="Record"
  args={{
    wide: false,
  }}
>
  {Template.bind({})}
</Story>
