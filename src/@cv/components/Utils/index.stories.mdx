import Component from "./FontTest";

import { Meta, Story } from "@storybook/addon-docs";

<!--👇 The title prop determines where your story goes in the story list -->

<Meta
  title="Utils"
  parameters={{
    backgrounds: {
      grid: {
        cellSize: 14,
        opacity: 0.5,
        cellAmount: 4,
        offsetX: 16,
        offsetY: 14,
      },
    },
  }}
  component={Component}
/>

<!--👇 We create a “template” of how args map to rendering -->

export const Template = (args) => <Component {...args} />;

<!-- 👇 The args you need here will depend on your component -->

<Story name="Font Test">{Template.bind({})}</Story>
