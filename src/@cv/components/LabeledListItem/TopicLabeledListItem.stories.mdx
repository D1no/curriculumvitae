import { TopicLabeledListItem } from "./index";

import { Meta, Story } from "@storybook/addon-docs";

<!--👇 The title prop determines where your story goes in the story list -->

<Meta title="Text Elements" component={TopicLabeledListItem} />

<!--👇 We create a “template” of how args map to rendering -->

export const Template = (args) => <TopicLabeledListItem {...args} />;

<!-- 👇 The args you need here will depend on your component -->

<Story
  name="(Topic) LabeledListItem"
  args={{
    label: "Empty Label",
    double: false,
    children:
      "This is a test to see if the line fits and does wrap to the next line for alignment.",
  }}
>
  {Template.bind({})}
</Story>
